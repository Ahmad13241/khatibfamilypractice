[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

# Ensure all deployed files keep their case sensitivity
[build.processing]
  skip_processing = true 